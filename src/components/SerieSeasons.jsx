import React from "react";
import {Accordion, AccordionItem, Card, Tab, Tabs} from "@nextui-org/react";

export default function SerieSeasons({seasons}) {
    return (
        <div className="flex flex-wrap justify-between">
            
        </div>
    );
}